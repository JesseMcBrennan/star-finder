
const chai = require('chai');
const should = chai.should();
const chaiHttp = require('chai-http');
const server = require('./server');

//remember to use done as an argument int he it block
chai.use(chaiHttp);

describe('Client Routes', () => {

});

describe('API Routes', () => {

});